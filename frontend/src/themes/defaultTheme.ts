import { createTheme } from "@mui/material";

export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: "#0C141C",
    },
    background: {
      default: "#F7F8FD",
    },
  },
});
